#
# define display information
#
name: EFI-GNT GND FASTA Sequence Lookup

tooltip: |
    Enzyme Function Initiative (EFI) Tools

screenshots: []

icon: icon.png

#
# define a set of similar apps that might be useful to the user
#
suggestions:
    apps:
        related: []
        next: []

#
# Configure the display and description of parameters
#
parameters:
    sequence_headers:
        ui-name: FASTA Headers (paste)
        short-hint: |
            The genomic neighborhoods are retreived for the UniProt, NCBI, EMBL-EBI ENA, and PDB identifiers that are identified in the FASTA headers. Not all identifiers may exist in the EFI-GNT database so the results will only include diagrams for sequences that were identified. 
            
    sequence_headers_upload:
        ui-name: FASTA Headers (upload)
        short-hint: |
            The genomic neighborhoods are retreived for the UniProt, NCBI, EMBL-EBI ENA, and PDB identifiers that are identified in the FASTA headers. Not all identifiers may exist in the EFI-GNT database so the results will only include diagrams for sequences that were identified. 

    neighborhood_window_size:
        ui-name: Neighborhood window size
        short-hint: |
            Number of neighbors to retrieve on either side of the query sequence for each BLAST result

description: |
    <p> <a href="https://efi.igb.illinois.edu/">Enzyme Function Initiative</a></p>
