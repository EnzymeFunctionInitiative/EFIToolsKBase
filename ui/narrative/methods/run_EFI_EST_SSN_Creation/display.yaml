#
# define display information
#
name: EFI-EST SSN Creation

tooltip: |
    Create an SSN from EFI-EST ComputedProteinSims workspace object

screenshots: []

icon: icon.png

#
# define a set of similar apps that might be useful to the user
#
#suggestions:
#    apps:
#        related:
#            [app1, app2]
#        next:
#            [app3, app4]

#
# Configure the display and description of parameters
#
parameters :
    blast_edge_file :
        ui-name : |
            Edge File
        short-hint : |
            The workspace object representing the SSN edges
        long-hint : |
            The workspace object representing the SSN edges
    alignment_score :
        ui-name : |
            Alignment score
        short-hint : |
            The alignment score used to group sequences into clusters
        long-hint : |
            The alignment score used to group sequences into clusters
    min_length:
        ui-name: Minimum Sequence Length
        short-hint: The minimum length required for a sequence to be included in the SSN
        long-hint: The minimum length required for a sequence to be included in the SSN
    max_length:
        ui-name: Maximum Sequence Length
        short-hint: The maximum length required for a sequence to be included in the SSN
        long-hint: The maximum length required for a sequence to be included in the SSN

description : |
    <p>Creates an SSN from a dataset from the initial computation step.</p>:w
